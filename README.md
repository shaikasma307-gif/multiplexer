# multiplexer
### Multiplexer — GitHub Description  A **Multiplexer (MUX)** is a digital circuit that **selects one input from multiple inputs and sends it to a single output** based on the select lines. It is commonly used for **data selection and routing** in digital systems.
